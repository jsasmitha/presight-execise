import { User } from "@interfaces/models/user.interface";

// Props interface for the UserCard component, which represents a card displaying user information
export interface UserCardProps {
  user: User;
}
